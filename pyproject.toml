[project]
name = "barnsight-edge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10,<3.12"
dependencies = [
    "dotenv",
    "lap>=0.5.12",
    "pydantic-settings>=2.12.0",
    "requests",
    "torch",
    "torchvision",
    "ultralytics>=8.3.246",
]

[tool.uv]
environments = [
  "sys_platform == 'linux'",
]

